<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="@dimen/universalMargin"
    app:cardCornerRadius="@dimen/universalMargin"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/whiteColor"
        android:padding="@dimen/universalMargin"
        android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/imgListSize"
        android:layout_margin="@dimen/bigMargin"
        >

        <fr.castorflex.android.circularprogressbar.CircularProgressBar
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:id="@+id/listProgressBar"
            android:layout_width="@dimen/progressBarSize"
            android:layout_height="@dimen/progressBarSize"
            android:layout_gravity="center"
            android:layout_marginRight="@dimen/smallMargin"
            android:indeterminate="true"
            app:cpb_color="@color/blackColor"
            app:cpb_max_sweep_angle="300"
            app:cpb_min_sweep_angle="10"
            app:cpb_rotation_speed="1.0"
            app:cpb_sweep_speed="1.0" />

        <ImageView
            android:id="@+id/imgViewThumbnail"
            android:layout_width="match_parent"
            android:layout_height="@dimen/imgListSize" />

    </FrameLayout>


    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_marginLeft="@dimen/bigMargin"
        android:layout_height="wrap_content"
        android:textSize="@dimen/textSize"
        android:textStyle="bold"
        android:textColor="@color/blackColor"
        android:layout_gravity="left"
        />

    <TextView
        android:id="@+id/textViewDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/smallTextSize"
        android:layout_marginLeft="@dimen/bigMargin"
        android:textColor="@color/blackColor"
        android:layout_gravity="left"
        />
    </LinearLayout>
</android.support.v7.widget.CardView>